const express = require('express');

const oauthRouter = express.Router();

// const { loginWithOauth } = require('./oauth.controller');

// oauthRouter.get('/auth/google', loginWithOauth);

module.exports = oauthRouter;